
Процедура КнопкаВыполнитьНажатие(Кнопка)
	ОчиститьСообщения();
	
	Если Не ЭтаФорма.ПроверитьЗаполнение() Тогда 
		Возврат;
	КонецЕсли; 	
		
	ЗавершитьСборку(ОбработкаОбъект.СсылкаНаОбъект);
	
	Сообщить("Процедура завершения сборки окончена.");
КонецПроцедуры
